# JobCity backend — runtime dependencies (+ a small test runner).
#
# The slim set the running app actually imports, plus transitive deps. The test
# runner (pytest etc.) is kept here too so CI's single `pip install -r
# requirements.txt` provides it; only linters/formatters live in
# requirements-dev.txt. The Dockerfile installs this file.
#
# emergentintegrations + litellm (AI summary/match) live in requirements-ai.txt —
# off by default and imported lazily, so the base build never depends on the
# vendor-hosted litellm wheel.

# --- Direct runtime imports (app code) ---
fastapi==0.110.1
uvicorn==0.25.0
starlette==0.37.2
pydantic==2.13.4
motor==3.3.1
pymongo==4.5.0
APScheduler==3.11.2
python-dotenv==1.2.2
httpx==0.28.1
bcrypt==4.1.3
PyJWT==2.13.0

# --- Transitive deps (pinned for reproducible installs) ---
annotated-types==0.7.0
anyio==4.13.0
certifi==2026.5.20
click==8.4.1
dnspython==2.8.0
h11==0.16.0
httpcore==1.0.9
idna==3.18
pydantic_core==2.46.4
typing-inspection==0.4.2
typing_extensions==4.15.0
tzlocal==5.4.3

# --- Test runner (kept in the runtime set so CI's single install provides
# pytest; adds only a few small pure-Python packages to the image) ---
pytest==9.0.3
pytest-asyncio==1.4.0
mongomock==4.3.0
mongomock-motor==0.0.36
pluggy==1.6.0
iniconfig==2.3.0
packaging==26.2
sentinels==1.1.1
